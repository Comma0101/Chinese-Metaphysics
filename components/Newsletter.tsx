/**
 * Newsletter signup — DISABLED (fail-closed) per the 2026-07-10 pilot
 * contract. Email collection stays off until an immutable consent /
 * unsubscribe / re-consent workflow and actual mailing operations exist.
 * Do not re-enable by rendering a form here or by bundling consent into
 * another checkbox. This component intentionally renders nothing.
 */
export function Newsletter() {
  return null;
}
